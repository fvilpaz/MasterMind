**Trainer:** ¡Nando, máquina! ¡Arrancamos con la energía a tope! La última vez dejamos el listón muy alto: dominaste los bucles anidados y la pirámide de Mario (More) como un campeón, y te lancé el reto "Hello, You" para empezar a explorar `string`, `get_string()` y `%s`. ¿Cuánto tiempo tienes hoy para seguir conquistando C: 1 hora (2 pomodoros), hora y media (3 pomodoros) o 2 horas (4 pomodoros)? ¡Vamos a por ese saludo personalizado!

**Estudiante:** #include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Pedir el nombre al usuario y guardarlo en una variable de tipo string
    string name = get_string("What is your name? ");

    // Saludar usando %s como placeholder para el texto
    printf("hello, %s\n", name);
}

**Trainer:** ¡Excelente, Nando! Tu código para "Hello, You" es perfecto y demuestra que has captado cómo usar `get_string` y la variable `string` junto con `printf` para personalizar el saludo. Has resuelto el reto de forma concisa y efectiva. Ahora, para asegurarnos de que lo dominas de verdad, ¿podrías explicarme por qué utilizamos específicamente `%s` para mostrar el nombre y qué crees que pasaría si intentáramos usar `%d` en su lugar?