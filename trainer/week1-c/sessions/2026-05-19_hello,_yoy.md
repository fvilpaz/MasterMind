**Trainer:** ¡Nando, máquina! ¡Vamos a darle caña a la sesión de hoy! La última vez dejamos el listón altísimo: ¡Mario (More) totalmente dominado y listo para el siguiente reto! Tienes el desafío "Hello, You" pendiente para que arranquemos con `string` y `printf`. ¿Cuánto tiempo tienes hoy para el fuego: 1 hora (2 pomodoros), hora y media (3 pomodoros) o 2 horas (4 pomodoros)? ¡Dime y arrancamos!

**Estudiante:** #include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Pedir el nombre al usuario y guardarlo en una variable de tipo string
    string name = get_string("What is your name? ");

    // Saludar usando %s como placeholder para el texto
    printf("hello, %s\n", name);
}

**Trainer:** ¡Nando, esto es **perfecto**! Has abordado el desafío "Hello, You" con una precisión impecable y un dominio claro de los conceptos.

Tu código:

1.  **Incluye las librerías necesarias**: `#include <cs50.h>` para `get_string` y `#include <stdio.h>` para `printf`. ¡Excelente!
2.  **Usa `get_string` correctamente**: Para pedir la entrada del usuario con el mensaje adecuado.
3.  **Declara una variable `string`**: `string name = ...` para almacenar el texto que el usuario introduce. Como bien sabrás, un `string` es un tipo de dato que usamos en C para almacenar cadenas de caracteres, es decir, ¡texto! Es como la "palabra" o "frase" que el usuario escribe.
4.  **Emplea `printf` con el formato correcto**: `printf("hello, %s\n", name);` Utilizas `%s` como un "placeholder" o marcador de posición que `printf` reemplazará con el valor de la variable `name` cuando el programa se ejecute. La `s` en `%s` es una pista directa: ¡es para `string`!

Si ejecutáramos este código, veríamos exactamente:

```
What is your name? Nando
hello, Nando
```

**¡Has demostrado un entendimiento sólido de cómo obtener entrada de texto del usuario, almacenar ese texto en una variable y luego mostrarlo en pantalla de forma dinámica!** ¡Felicidades!

Para consolidar esto y siguiendo el enfoque de Malan, ¿podrías explicarme brevemente: **"¿Por qué crees que es necesario usar `%s` dentro de las comillas en `printf` cuando queremos mostrar una variable de tipo `string`, en lugar de simplemente concatenar o poner la variable directamente?"**

Después de tu respuesta, podemos pasar a **modo socrático** para que me demuestres que lo entiendes a fondo, o podemos introducir otros tipos de datos y sus códigos de formato asociados.