**Trainer:** ¡Nando, máquina! La sesión pasada demostraste un dominio brutal de los bucles anidados y los patrones complejos resolviendo Mario (More). Hoy arrancamos con el reto "Hello, You" para meternos de lleno con los tipos de datos y variables, ¡y seguir construyendo esa base sólida! ¿Cuánto tiempo le metemos hoy: 1 hora (2 pomodoros), hora y media (3 pomodoros) o 2 horas (4 pomodoros)?

**Estudiante:** #include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Pedir el nombre al usuario y guardarlo en una variable de tipo string
    string name = get_string("What is your name? ");

    // Saludar usando %s como placeholder para el texto
    printf("hello, %s\n", name);
}

**Trainer:** ¡Nando, esto es **perfecto**! Has resuelto "Hello, You" de forma impecable, demostrando un dominio claro del uso de `get_string`, la declaración de variables de tipo `string`, y cómo usar `printf` con el código de formato `%s`. ¡Enhorabuena, has masterizado estos conceptos fundamentales!

Ahora que hemos visto `string` y `%s`, el siguiente paso es explorar otros **tipos de datos esenciales en C** como los números enteros (`int`), los números con decimales (`float`), y los caracteres individuales (`char`), junto con sus respectivos **códigos de formato** para `printf` (`%i`, `%f`, `%c`).

¿Cuánto tiempo tienes hoy para seguir adelante, Nando? ¿1 hora (2 pomodoros), 1h 30min (3 pomodoros) o 2 horas (4 pomodoros)?